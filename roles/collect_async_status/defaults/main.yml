---
cas_async_retries: "{{ controller_configuration_async_retries | default(30) }}"
cas_async_delay: "{{ controller_configuration_async_delay | default(1) }}"
cas_secure_logging: "{{ controller_configuration_secure_logging | default('false') }}"
...
