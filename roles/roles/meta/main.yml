---
galaxy_info:
  role_name: roles
  author: Tom Page
  description: An Ansible role to assign role based access controls on Ansible Controller objects to a given team.
  company: Red Hat
  license: GPL-3.0-or-later
  min_ansible_version: 2.16.0
  platforms:
    - name: EL
      versions:
        - all
  galaxy_tags:
    - controller
    - aap
    - awx
    - configuration
    - rbac

collections:
  - ctrliq.ascender

dependencies:
  - role: global_vars
  - role: meta_dependency_check
...
