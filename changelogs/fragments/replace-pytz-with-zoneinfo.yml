---
minor_changes:
  - schedule_rrule and schedule_rruleset lookup plugins - validate timezone names with the standard library ``zoneinfo`` module instead of ``pytz``, removing the third-party ``pytz`` requirement.
